export default defineNuxtRouteMiddleware((to, from) => {
    //   if (from.path !== "/") {
    //     return navigateTo("/");
    //   }
    //   else{
    //     return abortNavigation()
    //   }
    //   if(process.client) {
    // if (!process.server) {
    //   console.log(`${from.path} => ${to.path}`);
    // }
  });